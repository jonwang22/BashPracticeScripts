#!/bin/bash

# User Interaction: Write a script that asks the user for input (e.g., a username) and then checks if that user exists on the system. If the user doesn't exist, prompt to create a new user.


